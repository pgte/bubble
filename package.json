{
  "name": "bubble",
  "description": "Domains for the poor man. Flow-control for cascading callbacks. Error handling. Aborts groups of callbacks. With timeouts.",
  "version": "0.1.0",
  "author": {
    "name": "Pedro Teixeira"
  },
  "repository": {
    "type": "git",
    "url": "http://github.com/pgte/bubble.git" 
  },
  "tags": ["flow control", "error handling", "domains", "timeout", "callbacks"],
  "main": "./bubble.js",
  "engines": { "node": ">= 0.4.0" },
  "devDependencies": {
    "tap": "*"
  },
  "scripts": {
    "test": "node node_modules/tap/bin/tap.js test.js"
  }
}